import os,re
import SimpleITK as sitk
import numpy as np

# root = "/workspace/mnt/group/alg-pro/yankai/segment/data/pre_process_tumor"
#
# file_list = [file for file in os.listdir(root) if "segmentation" in file]
#
# for file in file_list:
#     image = sitk.ReadImage(os.path.join(root,file))
#     array = sitk.GetArrayFromImage(image)
#     if (np.max(array) == 0):
#         print(file)

a = 1
print(a.lower())